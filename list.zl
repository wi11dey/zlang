((List) ?a ?b ?c)
=> ((List UInt8) ?a ?b ?c)

((List) ?a ?b ?c "asdlfkjasdfklasdfj")
=> ((List ((List Type) UInt8 String)) ?a ?b ?c "asdlfkjasdfklasdfj")
=> '(?a ?b ?c "asdlfkjasdfklasdfj")

((List) UInt8 String)
=> ((List Type) UInt8 String)

(type-of (List ?a ?b ?c))
=> (List UInt8)

(define (List (type '(List) ))
  (lambda ()
    :math `"{}"
    ))

(define (insert! (x `(List ,T)) (newelement T)) ; TODO generics and type system?
  :math nil
  (malloc ))
